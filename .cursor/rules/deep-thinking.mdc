---
description: Optional pointer — use deep-analysis subagent instead of bloating every chat
globs:
alwaysApply: false
---

# Deep thinking (legacy)

The long “ultra deep” system prompt is **not** applied globally anymore.

- For exhaustive analysis, use Subagent **`deep-analysis`** (`.cursor/agents/deep-analysis.md`) or ask the user to request that mode explicitly.
