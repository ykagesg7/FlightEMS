---
description: Supabase learning_contents registration — trigger rule; full procedure in Skill
globs:
  - src/content/lessons/**/*.mdx
  - src/content/articles/**/*.mdx
  - scripts/database/**
alwaysApply: false
---

# Supabase article registration

When adding or updating lesson MDX that must appear in the app catalog, sync **`learning_contents`** via Skill **[`.cursor/skills/learning-contents-registration/SKILL.md`](../skills/learning-contents-registration/SKILL.md)** (MCP `execute_sql`, fallback scripts, CPL meta SQL list, schema table).

Keep **CPL `category`** as **`CPL学科`** for `PrevNextNav` consistency.
