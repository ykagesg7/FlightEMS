---
description: Project-wide defaults for FlightAcademyTsx (replaces legacy .cursorrules)
alwaysApply: true
---

# Core project rules

- Always respond in Japanese.
- Do not change package versions without explicit approval.
- Prefer type-safe code; avoid `any`.
- No UI/UX changes without approval (read `DESIGN.md` before visual work).
- Keep secrets in `.env.local`; never inline keys.
- Follow existing formatting and linter rules.
- Use React 18 patterns and existing Suspense boundaries.
- Prefer Supabase MCP for Supabase work (server name under **Settings → MCP**).
